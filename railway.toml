[deploy]
# Run the 24/7 scheduler (rotates niches and drafts one listing per day).
# main.py is invoked as a subprocess by scheduler.py.
startCommand = "python scheduler.py"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10